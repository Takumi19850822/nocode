name = "nocode"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# next-on-pages のビルド出力（Workers Builds の Deploy コマンドは pages deploy を使う）
pages_build_output_dir = ".vercel/output/static"
